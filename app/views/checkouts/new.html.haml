%div{class: "pure-g pure-u-1"}
  - if @offer
    %h1 Buy an Offer
    %div
      %h5 Name
      = @offer.name
    %div
      %h5 Description
      = @offer.description
    %div
      %h5 Amount
      = @offer.amount
      = form_tag(checkouts_path, class: "pure-form") do
        %fieldset
          %div{class: "pure-u-1-4"}
            = text_field_tag :coupon,nil, {class: "pure-input-1-4"}
            = number_field_tag :amount, @balance_remaining, {min: 0.0, max: @balance_remaining, class: "pure-input-1-4"}
            = hidden_field_tag :offer_id, @offer.id
            %button{class: "pure-button", type: "submit"}
              Buy it
    - if @invoice && @invoice.payments.count.positive?
      = render 'checkouts/invoice'
  - else
    %h2 There are no offers.

